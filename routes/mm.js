const express = require("express");

const router = express.Router();

const MMController = require("../controllers/mm");

// example of using routes
// router.get("/add-plant", MMController.getAddPlant);

module.exports = router;
